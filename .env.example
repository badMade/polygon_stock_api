# Stock Data Retrieval System - Environment Configuration
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env to version control!

# =============================================================================
# API CREDENTIALS
# =============================================================================

# Polygon.io API Key (required for real API calls)
# Get your key at: https://polygon.io/dashboard/api-keys
# Format: alphanumeric string
POLYGON_API_KEY=your_polygon_api_key_here

# Notion Integration Token (required for Notion uploads)
# Create at: https://www.notion.so/my-integrations
# Format: starts with "secret_"
NOTION_API_KEY=secret_your_notion_token_here

# Notion Database ID (required for Notion uploads)
# Found in database URL: https://notion.so/workspace/{DATABASE_ID}?v=...
NOTION_DATABASE_ID=638a8018f09d4e159d6d84536f411441

# =============================================================================
# APPLICATION PATHS
# =============================================================================

# Base data directory for all input/output files
# Default: ./user-data (relative to script location)
# STOCK_APP_DATA_DIR=/path/to/your/data/directory

# =============================================================================
# PROCESSING CONFIGURATION (Optional - defaults shown)
# =============================================================================

# Number of tickers to process per batch
# Higher = faster but more memory usage
# Default: 100
# BATCH_SIZE=100

# Delay between API calls in seconds (rate limiting)
# Lower = faster but may hit rate limits
# Default: 0.01 (10ms = 100 requests/second max)
# RATE_LIMIT_DELAY=0.01

# Save checkpoint every N batches
# Lower = more frequent saves, safer but slower
# Default: 10
# CHECKPOINT_INTERVAL=10

# =============================================================================
# LOGGING (Optional)
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Log file path (leave empty for console only)
# LOG_FILE=/path/to/logfile.log

# =============================================================================
# DEVELOPMENT/TESTING (Optional)
# =============================================================================

# Enable debug mode (more verbose output)
# DEBUG=false

# Limit number of tickers for testing
# Set to small number for quick tests
# TICKER_LIMIT=10

# Skip actual API calls (use placeholder data)
# Useful for testing without API credentials
# DRY_RUN=false
