# Include runtime and test dependencies
-r requirements.txt

# Additional development tools (if needed beyond requirements.txt)
# All core dependencies are now in requirements.txt including:
# - pytest, pytest-cov (testing)
# - flake8, mypy, bandit (linting, type checking, security)
# - notion-client, polygon-api-client (API integrations)
